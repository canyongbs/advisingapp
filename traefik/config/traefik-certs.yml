tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certificates/cgbs-local-dev.pem
        keyFile: /certificates/cgbs-local-dev-key.pem
  certificates:
    - certFile: /certificates/cgbs-local-dev.pem
      keyFile: /certificates/cgbs-local-dev-key.pem
      stores:
        - default