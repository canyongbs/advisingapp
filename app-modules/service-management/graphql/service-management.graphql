type ServiceRequest
    @model(class: "AdvisingApp\\ServiceManagement\\Models\\ServiceRequest") {
    "Unique primary key."
    id: UUID!

    # TODO: Finish fields
}

input ServiceRequestQuery {
    "Unique primary key."
    id: UUID
}

# TODO: Query and Mutate
